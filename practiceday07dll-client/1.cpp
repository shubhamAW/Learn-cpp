#include<iostream>
#include<string>
#include<utility>
using namespace std;

#include "Header.h"

int main() {
	/*int a = 2, b = 3;
	cout << get_add(&a, &b);*/
	
	//2
	/*double num = 5.6;
	double integer = 0;
	double fraction = modf(num, &integer);

	cout << integer<<endl;
	cout << fraction<<endl;*/

	//3
	/*string ch;
	getline(cin,ch);

	int count_a = 0, count_e = 0, count_i = 0, count_o = 0, count_u = 0;
	count_vowels(&ch[0], &count_a, &count_e, &count_i, &count_o, &count_u);

*/

	//4

	/*int arr[] = { 5,7,2,9,8 };
	int size = sizeof(arr) / sizeof(arr[0]);

	int min = 0, max = 0, sum = 0, avg = 0;
	get_statistic_data(arr, size, &min, &max, &sum, &avg);
	
	cout << min;*/

	//5 

	/*double a = 5, b = 6, c = 1, root1 = 0, root2 = 0; 
	 roots(a, b, c,root1,root2);
	 cout << root1 << endl;
	 cout << root2 << endl;*/

	//6




}